
#include <iostream>
using namespace std;
int main() {
    int row, col,num;
    cout<<"Enter the Number of rows in 2D Array "<<endl;
    cin>>row;
     cout<<"Enter the Number of columns in 2D Array "<<endl;
     cin>>col;
     cout<<"Enter the number to search in the Array "<<endl;
     cin>>num;
     cout<<"Enter the elements  in 2D Array "<<endl;
  int arr[row][col];
  for(int i=0;i<row;i++)
  {
      for(int j=0;j<col;j++)
      {
          cin>>arr[i][j];
      }
  }
  for(int i=0;i<row;i++)
  {
      for(int j=0;j<col;j++)
      {
          if(arr[i][j]==num)
          {
    cout<<num<<" is Present in this Array ";
              return 0;
          }
      }
      cout<<endl;
  }
  cout<<num<<" is not Present in this Array ";

    return 0;
}
