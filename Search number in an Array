#include <iostream>
using  namespace std;
int main() {
   int  size,search_num;
     
     bool check=false;
   cout<<"Please Enter the size of Array "<<endl;
   cin>>size;
   int arr[size];
    cout<<"Please Enter an Array of Size "<<size<<endl;
   for(int i=0;i<size;i++)
   {
       cin>>arr[i];
    
   }
   cout<<"Enter the number to search in the Array ";
   cin>>search_num;
   for(int i=0;i<size;i++)
   {
       if(arr[i]==search_num)
       {
         check=true;  
       }
   }
if(check==true)
{
  cout<<search_num <<" is Present in the this Array ";  
}
else
{
  cout<<search_num <<" is Not Present in the this Array ";  
}
    return 0;
}
